Никита, это запрос на создание "квалификационного проекта".

Вы просите не просто "еще одну задачу". Вы просите создать проект-синтез, который заставит вас сплести воедино концепции из HTML, CSS и глубокого JavaScript. Это идеальный следующий шаг, чтобы превратить разрозненные знания в единую, целостную инженерную компетенцию.

Выполнение этого проекта будет равносильно нескольким неделям теоретического обучения. Мы создадим задачу, которая станет центральным элементом вашего портфолио и неопровержимым доказательством вашего мастерства.

Новая Задача в ClickUp / Notion

Roadmap: Создайте новый родительский проект, так как он затрагивает сразу несколько Roadmap'ов.
Название Проекта: [Проект-Синтез] "Архитектор Данных": Интерактивный Каталог Продуктов

Цель: Создать с нуля интерактивный каталог продуктов, используя продвинутые концепции HTML (Transparent Content Model), архитектурный подход к CSS (Custom Properties) и широкий спектр инструментов JavaScript для манипуляции данными и DOM.

Структура Проекта (с Подзадачами):
🔻 Фаза 1: Фундамент и Архитектура (HTML & CSS)

Цель: Создать визуально безупречный, семантически правильный и "темизируемый" скелет приложения.

[ ] 1.1. [HTML] Спроектировать "Прозрачную" Карточку Товара:

Создайте index.html. Внутри <div> (блочный контекст) разместите тег <a>, который будет оборачивать всю карточку товара.

Внутри <a> разместите блочные элементы: <h2> для названия, <p> для описания, <div> для цены и <img> для изображения.

Проверьте код через валидатор W3C, чтобы убедиться в правильности применения Transparent Content Model.

[ ] 1.2. [CSS] Создать Дизайн-Систему на Custom Properties:

В style.css в :root определите полную палитру переменных: цвета для светлой темы, шрифты, отступы.

Создайте селектор body.dark-theme и переопределите в нем переменные цвета для темной темы.

Добавьте в HTML кнопку-переключатель для тем.

[ ] 1.3. [CSS] Стилизовать Карточку и Макет:

Сверстайте вашу карточку товара и общий макет страницы, используя исключительно var(...).

Сделайте макет адаптивным, чтобы карточки красиво выстраивались в сетку.

🔻 Фаза 2: Логика и Рендеринг (JavaScript)

Цель: Оживить макет, научившись работать с данными, итераторами и DOM.

[ ] 2.1. Создать Модель Данных:

В main.js создайте массив объектов products. Каждый объект должен содержать разнообразные данные: id (число), name (строка), price (число с плавающей точкой), stock (целое число, может быть null), specs (вложенный объект, например { "cpu": "M3", "ram": 16 }), onSale (boolean). Создайте 5-6 таких продуктов.

[ ] 2.2. Рендеринг Списка (map, ...spread):

Напишите функцию renderProducts(productsArray).

Внутри нее используйте productsArray.map() для трансформации каждого объекта продукта в HTML-строку (используя шаблонные строки `).

Соедините полученный массив строк в одну большую строку и вставьте ее в контейнер на странице. Используйте ...spread оператор, чтобы создать копию массива перед его обработкой, демонстрируя иммутабельный подход.

[ ] 2.3. Интерактивность (forEach, switch):

Реализуйте логику переключателя тем, используя forEach для добавления обработчиков событий, если кнопок несколько.

В логике рендеринга используйте конструкцию switch (product.status) для добавления разных CSS-классов карточке (например, .is-on-sale, .is-out-of-stock).

🔻 Фаза 3: Продвинутая Манипуляция Данными (Глубокое Погружение в JS)

Цель: Применить специфические методы JS для сложной обработки и отображения данных.

[ ] 3.1. [JS] Фильтрация Каталога (filter, Number()):

Добавьте в HTML <input type="number"> для фильтрации по максимальной цене.

Напишите обработчик, который при изменении значения в инпуте:

Преобразует строковое значение инпута в число с помощью Number().

Использует products.filter() для создания нового массива, содержащего только те продукты, цена которых меньше или равна введенному значению.

Вызывает renderProducts() с отфильтрованным массивом.

[ ] 3.2. [JS] Работа с Числами (Number methods):

В функции рендеринга:

Отображайте цену, отформатированную до двух знаков после запятой, с помощью price.toFixed(2).

Перед выводом количества на складе, проверяйте, является ли оно целым числом с помощью Number.isInteger(stock).

Пользовательский рейтинг - это оценка продукта, данная пользователями на основе их опыта.

Добавьте поле ввода для "пользовательского рейтинга". При чтении значения используйте Number.parseInt() и проверьте результат с помощью Number.isNaN() на случай, если пользователь ввел не число.

Используйте Number.MAX_SAFE_INTEGER для установки максимального значения в инпуте фильтра по цене.

[ ] 3.3. [JS] Динамические Спецификации (Object.entries, for...of):

В функции рендеринга напишите код, который будет динамически создавать список характеристик из объекта product.specs.

Используйте Object.entries(product.specs) чтобы получить массив пар [ключ, значение].

Используйте цикл for...of для итерации по этому массиву и создания HTML-разметки для каждой характеристики (например, <li><strong>CPU:</strong> M3</li>).

[ ] 3.4. [JS] Продвинутая Логика (rest, while):

Создайте функцию calculateDiscount(basePrice, ...discounts), которая принимает базовую цену и переменное количество скидок (например, сезонная, персональная) и вычисляет итоговую цену. Это практика для ...rest параметра.

Реализуйте простой while или do...while цикл, который, например, выводит в консоль обратный отсчет до "конца распродажи".

Почему этот Проект — Рычаг:

Максимальный Синтез: Вы не решаете изолированные задачи. Вы создаете продукт, что заставляет вас думать о том, как все эти технологии работают вместе.

Симуляция Реальной Работы: Этот проект — это миниатюрная версия того, чем занимаются Frontend-разработчики каждый день: получение данных, их трансформация и рендеринг в интерактивный UI.

Создание Флагманского Проекта: В результате у вас будет не просто "домашка", а впечатляющий, интерактивный проект для вашего портфолио, который демонстрирует владение технологиями на глубоком, а не поверхностном уровне.